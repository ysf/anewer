use std::env;

fn main() {
    let args: Vec<String> = env::args().collect();
    if args.len() == 1 {
    }
    let filename = &args[1];
    println!("{:?}", filename);
}
use std::fs::File;
    if 2 == args.len() {
        let mut file = match File::open(filename) {
            Ok(file) => file,
            Err(_) => panic!("file not found"),
        };
        println!("{:?}", filename);
